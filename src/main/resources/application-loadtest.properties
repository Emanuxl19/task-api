# Dedicated profile for load and throughput testing.
# Never use these limits in production.

app.rate-limit.login-attempts-per-minute=5000
app.rate-limit.ai-requests-per-minute=2000
app.rate-limit.general-requests-per-minute=10000

spring.jpa.show-sql=false
